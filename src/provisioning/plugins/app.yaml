apiVersion: 1

apps:
  - type: 'grafana-exploremetrics-app'
    org_id: 1
    org_name: 'grafana'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
      isApiKeySet: true
    secureJsonData:
      apiKey: secret-key
